# xAssassin

This repository stores soccer-related resources and data snapshots.

## Security

- Keep credentials in local `.env` files only.
- Never commit Supabase service-role keys.
- If keys were exposed previously, rotate them immediately in Supabase.
